import React from "react";

import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Pricing - jkbicierro",
  description: "Explore the pricing options for John Bicierro's services.",
};

export default function Page() {
  return <div>page</div>;
}
